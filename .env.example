# RemitProof's investigator runs locally through Ollama.
OLLAMA_HOST=http://127.0.0.1:11434
OLLAMA_MODEL=llama3.2
# Explicitly opt in on the backend server to allow sandbox model calls.
# Keep false on public deployments until model access and rate limits are configured.
SANDBOX_LIVE_AI_ENABLED=false
